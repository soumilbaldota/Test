import React from "react";
import "./header.css";
export default function Header() {
  return (
    <header
      className="bg-[conic-gradient(at_top,_var(--tw-gradient-stops))] from-gray-700 via-gray-900 to-black text-3xl font-semibold  text-white animate-focus-in-contract-bck"
      style={{ fontFamily: "Source Sans Pro" }}
    >
      EMPLOYEES
    </header>
  );
}
